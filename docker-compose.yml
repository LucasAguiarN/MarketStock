version: '3'
services:
  web:
    build:
      context: . 
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - .:/src
      - ./static/uploads:/src/static/uploads
    environment:
      FLASK_APP: run.py
      FLASK_ENV: development